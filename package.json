{
  "scripts": {
    "start": "react-scripts start",
    "build": "react=scripts build",
    "deploy": "gh-pages -d build"
  },
  "devDependencies": {
    "gh-pages": "^4.0.0"
  },
  "homepage": "https://seo-faper.github.io/Toy_Project_Nonogram/",
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  }
}
